import React from 'react'
import "../styles/Extra.css"

function Extra({text}) {
    return (
        <div className="extra">
            <a href="#">{text}</a>
        </div>
    )
}

export default Extra
